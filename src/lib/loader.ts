/**
 * @file loader.ts
 * @summary Handles auth, and data loading.
 *
 * @author Xin (Daniel) Feng
 * @contact intra: @xifeng
 */

import type { Table } from '@/lib/table';

/**
 * @returns Handles auth, and data loading.
 */
const loader = async (start: string | null, end: string | null): Promise<Table | null> => {
  return null;
};

export default loader;
